<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error de Validación | AgroTrack</title>
    <link rel="stylesheet" href="/estilos.css">
</head>
<body>
    <div class="container">
        <h1 class="titulo">Error en el formulario</h1>

        <% if (typeof errores !== "undefined" && errores.length > 0) { %>
            <div class="error-lista">
                <h3>Se encontraron los siguientes errores:</h3>
                <ul>
                    <% errores.forEach(function(error) { %>
                        <li><%= error %></li>
                    <% }) %>
                </ul>
            </div>
        <% } %>

        <h3>Datos ingresados:</h3>
        <div class="datos-previos">
            <p><strong>Nombre:</strong> <%= valores?.nombre || '' %></p>
            <p><strong>Email:</strong> <%= valores?.email || '' %></p>
            <p><strong>Mensaje:</strong> <%= valores?.mensaje || '' %></p>
        </div>

        <div class="acciones">
            <a href="/contacto.html" class="btn">Volver al formulario</a>
            <a href="/" class="btn btn-secundario">Volver al inicio</a>
        </div>
    </div>
</body>
</html>

